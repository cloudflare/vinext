name: Preview Release

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: {}

jobs:
  publish:
    # Skip for forks — they don't have write access needed by pkg-pr-new.
    if: github.repository_owner == 'cloudflare'
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/build-vinext
      - run: pnpm dlx pkg-pr-new publish --pnpm './packages/vinext'
