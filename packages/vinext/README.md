# vinext

Run Next.js apps on Vite. Drop-in replacement for the `next` CLI.

This repository is a monorepo; the published package lives in `packages/vinext`.

## Install

```bash
npm i vinext
```

## Usage

```bash
vinext --help
```
