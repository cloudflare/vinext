/** KV binding name vinext's runtime cache adapter looks up at request time. */
export const VINEXT_KV_CACHE_BINDING = "VINEXT_KV_CACHE";

/** Placeholder KV namespace id written until the user runs `wrangler kv namespace create`. */
export const KV_NAMESPACE_ID_PLACEHOLDER = "<your-kv-namespace-id>";

/** Default Cloudflare Images binding name when a config doesn't already define one. */
export const DEFAULT_IMAGES_BINDING = "IMAGES";
