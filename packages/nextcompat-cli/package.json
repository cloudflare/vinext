{
  "name": "nextcompat",
  "version": "0.0.1",
  "description": "CLI for nextcompat — run Next.js apps on Vite",
  "type": "module",
  "bin": {
    "nextcompat": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "vite": "^7.0.0",
    "vite-plugin-nextcompat": "workspace:*"
  }
}
