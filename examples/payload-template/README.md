# Payload on vinext

Minimal Payload 3 example running through vinext.

```bash
pnpm dev
```

Open `http://localhost:3000/admin` and create the first user. The example uses SQLite at `payload-template.db` by default.
