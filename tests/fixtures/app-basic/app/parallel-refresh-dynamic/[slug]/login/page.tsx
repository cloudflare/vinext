import { ParallelRefreshButton } from "../../../parallel-refresh/refresh-button";

export default function ParallelRefreshDynamicLoginPage() {
  return (
    <main data-testid="parallel-refresh-dynamic-login-page">
      <ParallelRefreshButton />
    </main>
  );
}
