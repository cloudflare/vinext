{
  "name": "nextcompat-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "fixtures/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/vite-plugin-nextcompat",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "playwright": "^1.52.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.1"
  },
  "dependencies": {
    "@unpic/react": "^1.0.2"
  }
}
