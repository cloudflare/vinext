diff --git a/packages/vinext/src/entries/app-rsc-entry.d.ts b/packages/vinext/src/entries/app-rsc-entry.d.ts
index b320d29f1983dadd0cf292616322585f62bd1ac9..3b01916eb8c0dbf05af941000c4e8169ee46b757 100644
--- a/packages/vinext/src/entries/app-rsc-entry.d.ts
+++ b/packages/vinext/src/entries/app-rsc-entry.d.ts
@@ -38,7 +38,7 @@ type AppRouterConfig = {
  * It matches the incoming request URL to an app route, builds the
  * nested layout + page tree, and renders it to an RSC stream.
  */
-declare function generateRscEntry(appDir: string, routes: AppRoute[], middlewarePath?: string | null, metadataRoutes?: MetadataFileRoute[], globalErrorPath?: string | null, basePath?: string, trailingSlash?: boolean, config?: AppRouterConfig, instrumentationPath?: string | null): string;
+declare function generateRscEntry(appDir: string, routes: AppRoute[], middlewarePath?: string | null, metadataRoutes?: MetadataFileRoute[], globalErrorPath?: string | null, basePath?: string, trailingSlash?: boolean, config?: AppRouterConfig, instrumentationPath?: string | null, root?: string): string;
 //#endregion
 export { generateRscEntry };
 //# sourceMappingURL=app-rsc-entry.d.ts.map
diff --git a/packages/vinext/src/entries/app-rsc-entry.ts b/packages/vinext/src/entries/app-rsc-entry.ts
index dacb370ab8044431a554f0133cd4ad49daba81e6..74b03baef17838c9e88fed7b07328fdcd9036b95 100644
--- a/packages/vinext/src/entries/app-rsc-entry.ts
+++ b/packages/vinext/src/entries/app-rsc-entry.ts
@@ -2,6 +2,8 @@ import { normalizePathSeparators, resolveEntryPath } from "./runtime-entry-modul
 import { isProxyFile } from "../server/middleware.ts";
 import { buildAppRscManifestCode } from "./app-rsc-manifest.ts";
 import { generateDevOriginCheckCode } from "../server/dev-origin-check.ts";
+import path from "node:path";
+import { MIME_TYPES } from "../server/mime.ts";
 //#region src/entries/app-rsc-entry.ts
 /**
 * App Router RSC entry generator.
@@ -44,7 +46,7 @@ const appHookWarningSuppressionPath = resolveEntryPath("../server/app-hook-warni
 * It matches the incoming request URL to an app route, builds the
 * nested layout + page tree, and renders it to an RSC stream.
 */
-function generateRscEntry(appDir, routes, middlewarePath, metadataRoutes, globalErrorPath, basePath, trailingSlash, config, instrumentationPath) {
+function generateRscEntry(appDir, routes, middlewarePath, metadataRoutes, globalErrorPath, basePath, trailingSlash, config, instrumentationPath, root) {
 	const bp = basePath ?? "";
 	const ts = trailingSlash ?? false;
 	const redirects = config?.redirects ?? [];
@@ -59,6 +61,7 @@ function generateRscEntry(appDir, routes, middlewarePath, metadataRoutes, global
 	const expireTime = config?.expireTime ?? DEFAULT_EXPIRE_TIME;
 	const i18nConfig = config?.i18n ?? null;
 	const hasPagesDir = config?.hasPagesDir ?? false;
+	const publicDir = root ? path.resolve(root, "public") : null;
 	const publicFiles = config?.publicFiles ?? [];
 	const { imports, routeEntries, metaRouteEntries, generateStaticParamsEntries, rootNotFoundVar, rootForbiddenVar, rootUnauthorizedVar, rootLayoutVars, globalErrorVar } = buildAppRscManifestCode({
 		routes,
@@ -72,6 +75,8 @@ async function __loadPrerenderPagesRoutes() {
 }
 ` : "";
 	return `
+import __nodeFs from "node:fs";
+import __nodePath from "node:path";
 import {
   renderToReadableStream as _renderToReadableStream,
   decodeAction,
@@ -305,6 +310,8 @@ const __configHeaders = ${JSON.stringify(headers)};
 const __publicFiles = new Set(${JSON.stringify(publicFiles)});
 const __allowedOrigins = ${JSON.stringify(allowedOrigins)};
 const __expireTime = ${JSON.stringify(expireTime)};
+const __mimeTypes = ${JSON.stringify(MIME_TYPES)};
+const __publicDir = ${JSON.stringify(publicDir)};
 
 ${generateDevOriginCheckCode(config?.allowedDevOrigins)}
 
@@ -629,6 +636,37 @@ export default __createAppRscHandler({
   metadataRoutes,
   middlewareModule: ${middlewarePath ? "middlewareModule" : "null"},
   publicFiles: __publicFiles,
+  resolveStaticFileAfterRewrite(cleanPathname, middlewareContext) {
+    const __staticPathname = cleanPathname.split("?")[0];
+    const __staticExtname = __nodePath.extname(__staticPathname);
+    if (!__staticExtname || __publicDir === null) return null;
+
+    const __publicFile = __nodePath.resolve(__publicDir, "." + __staticPathname);
+    if (!__publicFile.startsWith(__publicDir + __nodePath.sep)) return null;
+
+    try {
+      const __stat = __nodeFs.statSync(__publicFile);
+      if (!__stat.isFile()) return null;
+
+      const __content = __nodeFs.readFileSync(__publicFile);
+      const __ext = __staticExtname.slice(1).toLowerCase();
+      const __headers = new Headers({ "Content-Type": __mimeTypes[__ext] ?? "application/octet-stream" });
+      if (middlewareContext.headers) {
+        for (const [__key, __value] of middlewareContext.headers) {
+          const __lowerKey = __key.toLowerCase();
+          if (__lowerKey === "set-cookie" || __lowerKey === "vary") __headers.append(__key, __value);
+          else __headers.set(__key, __value);
+        }
+      }
+      return new Response(__content, {
+        status: middlewareContext.status ?? 200,
+        headers: __headers,
+      });
+    } catch (__e) {
+      if (__e?.code !== "ENOENT") console.warn("[vinext] static file check failed:", __e);
+      return null;
+    }
+  },
   renderNotFound({ isRscRequest, matchedParams, middlewareContext, request, route, scriptNonce }) {
     return __fallbackRenderer.renderNotFound(route, isRscRequest, request, matchedParams, scriptNonce, middlewareContext);
   },
diff --git a/packages/vinext/src/index.ts b/packages/vinext/src/index.ts
index 74073fdda3cfd863efc4a4d95fd0c053fb573b39..ebad57f2d81bb7daa3ab7d6c5aea45aa8576dc57 100644
--- a/packages/vinext/src/index.ts
+++ b/packages/vinext/src/index.ts
@@ -20,6 +20,7 @@ import { scanMetadataFiles } from "./server/metadata-routes.ts";
 import { shouldInvalidateAppRouteFile } from "./server/dev-route-files.ts";
 import { createDirectRunner } from "./server/dev-module-runner.ts";
 import { validateDevRequest } from "./server/dev-origin-check.ts";
+import { mimeType } from "./server/mime.ts";
 import { generateRscEntry } from "./entries/app-rsc-entry.ts";
 import { generateSsrEntry } from "./entries/app-ssr-entry.ts";
 import { generateBrowserEntry } from "./entries/app-browser-entry.ts";
@@ -755,7 +756,7 @@ function vinext(options = {}) {
 						i18n: nextConfig?.i18n,
 						hasPagesDir,
 						publicFiles: scanPublicFileRoutes(root)
-					}, instrumentationPath);
+					}, instrumentationPath, root);
 				}
 				if (id === RESOLVED_ROOT_PARAMS) return generateRootParamsModule((hasAppDir ? await appRouter(appDir, nextConfig?.pageExtensions, fileMatcher) : []).flatMap((route) => route.rootParamNames ?? []));
 				if (id === RESOLVED_APP_SSR_ENTRY && hasAppDir) return generateSsrEntry(hasPagesDir);
@@ -1159,9 +1160,35 @@ function vinext(options = {}) {
 								return;
 							}
 							const routes = await pagesRouter(pagesDir, nextConfig?.pageExtensions, fileMatcher);
+							const resolvedPublicDir = path.resolve(root, "public");
 							if (nextConfig?.rewrites.afterFiles.length) {
 								const afterRewrite = applyRewrites(resolvedUrl.split("?")[0], nextConfig.rewrites.afterFiles, reqCtx);
-								if (afterRewrite) resolvedUrl = afterRewrite;
+								if (afterRewrite) {
+									resolvedUrl = afterRewrite;
+									if (isExternalUrl(afterRewrite)) {
+										applyDeferredMwHeaders();
+										applyMwRequestHeadersForExternalProxy();
+										await proxyExternalRewriteNode(req, res, afterRewrite);
+										return;
+									}
+									const afterFilesPathname = afterRewrite.split("?")[0];
+									if (path.extname(afterFilesPathname)) {
+										const publicFilePath = path.resolve(resolvedPublicDir, "." + afterFilesPathname);
+										if (publicFilePath.startsWith(resolvedPublicDir + path.sep)) try {
+											const stat = fs.statSync(publicFilePath);
+											if (stat.isFile()) {
+												const content = fs.readFileSync(publicFilePath);
+												const ext = path.extname(afterFilesPathname).slice(1).toLowerCase();
+												applyDeferredMwHeaders();
+												res.writeHead(200, { "Content-Type": mimeType(ext) });
+												res.end(content);
+												return;
+											}
+										} catch (e) {
+											if (typeof e === "object" && e !== null && "code" in e && e.code !== "ENOENT") console.warn("[vinext] static file check failed:", e);
+										}
+									}
+								}
 							}
 							if (isExternalUrl(resolvedUrl)) {
 								applyDeferredMwHeaders();
@@ -1186,6 +1213,23 @@ function vinext(options = {}) {
 										await proxyExternalRewriteNode(req, res, fallbackRewrite);
 										return;
 									}
+									const fallbackPathname = fallbackRewrite.split("?")[0];
+									if (path.extname(fallbackPathname)) {
+										const publicFilePath = path.resolve(resolvedPublicDir, "." + fallbackPathname);
+										if (publicFilePath.startsWith(resolvedPublicDir + path.sep)) try {
+											const stat = fs.statSync(publicFilePath);
+											if (stat.isFile()) {
+												const content = fs.readFileSync(publicFilePath);
+												const ext = path.extname(fallbackPathname).slice(1).toLowerCase();
+												applyDeferredMwHeaders();
+												res.writeHead(200, { "Content-Type": mimeType(ext) });
+												res.end(content);
+												return;
+											}
+										} catch (e) {
+											if (typeof e === "object" && e !== null && "code" in e && e.code !== "ENOENT") console.warn("[vinext] static file check failed:", e);
+										}
+									}
 									if (!matchRoute(fallbackRewrite.split("?")[0], routes) && hasAppDir) return next();
 									applyDeferredMwHeaders();
 									if (middlewareRequestHeaders) applyRequestHeadersToNodeRequest(middlewareRequestHeaders);
@@ -1194,7 +1238,11 @@ function vinext(options = {}) {
 								}
 							}
 							if (hasAppDir) return next();
-							await handler(req, res, resolvedUrl, mwStatus);
+							try {
+								await handler(req, res, resolvedUrl, mwStatus);
+							} catch (e) {
+								next(e);
+							}
 						} catch (e) {
 							next(e);
 						}
diff --git a/packages/vinext/src/server/app-rsc-handler.d.ts b/packages/vinext/src/server/app-rsc-handler.d.ts
index 23182fe2c21a2ed4802e8da64d3145dca6b7fa49..a0477c856159e3331a93e363cb5d591b23da4d0d 100644
--- a/packages/vinext/src/server/app-rsc-handler.d.ts
+++ b/packages/vinext/src/server/app-rsc-handler.d.ts
@@ -105,6 +105,7 @@ type CreateAppRscHandlerOptions<TRoute extends AppRscHandlerRoute> = {
   publicFiles: ReadonlySet<string>;
   renderNotFound: (options: RenderNotFoundOptions<TRoute>) => Promise<Response | null>;
   renderPagesFallback?: (options: RenderPagesFallbackOptions) => Promise<Response | null>;
+  resolveStaticFileAfterRewrite?: (cleanPathname: string, middlewareContext: AppRscMiddlewareContext) => Response | null;
   rootParamNamesByPattern?: RootParamNamesMap;
   setNavigationContext: (context: NavigationContextValue) => void;
   staticParamsMap: StaticParamsMap;
diff --git a/packages/vinext/src/server/app-rsc-handler.ts b/packages/vinext/src/server/app-rsc-handler.ts
index a3a6b673f85a940beb052a028ed282899cbdfef1..3fdd31144173cb41a55738f50c83ea3ffb7cedb6 100644
--- a/packages/vinext/src/server/app-rsc-handler.ts
+++ b/packages/vinext/src/server/app-rsc-handler.ts
@@ -164,7 +164,14 @@ async function handleAppRscRequest(options, request, preMiddlewareRequestContext
 		rewrites: options.configRewrites.afterFiles
 	}, cleanPathname);
 	if (afterFilesRewrite instanceof Response) return afterFilesRewrite;
-	if (afterFilesRewrite) cleanPathname = afterFilesRewrite;
+	if (afterFilesRewrite) {
+		cleanPathname = afterFilesRewrite;
+		const staticFileResponse = options.resolveStaticFileAfterRewrite?.(cleanPathname, middlewareContext);
+		if (staticFileResponse) {
+			options.clearRequestContext();
+			return staticFileResponse;
+		}
+	}
 	let match = options.matchRoute(cleanPathname);
 	if (!match) {
 		const fallbackRewrite = await applyRewrite({
@@ -173,12 +180,17 @@ async function handleAppRscRequest(options, request, preMiddlewareRequestContext
 			requestContext: postMiddlewareRequestContext,
 			rewrites: options.configRewrites.fallback
 		}, cleanPathname);
-		if (fallbackRewrite instanceof Response) return fallbackRewrite;
-		if (fallbackRewrite) {
-			cleanPathname = fallbackRewrite;
-			match = options.matchRoute(cleanPathname);
+			if (fallbackRewrite instanceof Response) return fallbackRewrite;
+			if (fallbackRewrite) {
+				cleanPathname = fallbackRewrite;
+				const staticFileResponse = options.resolveStaticFileAfterRewrite?.(cleanPathname, middlewareContext);
+				if (staticFileResponse) {
+					options.clearRequestContext();
+					return staticFileResponse;
+				}
+				match = options.matchRoute(cleanPathname);
+			}
 		}
-	}
 	if (!match) {
 		const pagesFallbackResponse = await options.renderPagesFallback?.({
 			isRscRequest,
diff --git a/packages/vinext/src/server/mime.d.ts b/packages/vinext/src/server/mime.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c7dffa0a459bf85e918d0e6d2bc2b52ac0ea964e
--- /dev/null
+++ b/packages/vinext/src/server/mime.d.ts
@@ -0,0 +1,3 @@
+declare const MIME_TYPES: Record<string, string>;
+declare function mimeType(ext: string): string;
+export { MIME_TYPES, mimeType };
diff --git a/packages/vinext/src/server/mime.ts b/packages/vinext/src/server/mime.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2398753657b162c9946a7498e3b8b4032ae94d29
--- /dev/null
+++ b/packages/vinext/src/server/mime.ts
@@ -0,0 +1,36 @@
+const MIME_TYPES = {
+	html: "text/html; charset=utf-8",
+	htm: "text/html; charset=utf-8",
+	css: "text/css",
+	js: "application/javascript",
+	mjs: "application/javascript",
+	cjs: "application/javascript",
+	json: "application/json",
+	txt: "text/plain",
+	xml: "application/xml",
+	svg: "image/svg+xml",
+	png: "image/png",
+	jpg: "image/jpeg",
+	jpeg: "image/jpeg",
+	gif: "image/gif",
+	webp: "image/webp",
+	avif: "image/avif",
+	ico: "image/x-icon",
+	woff: "font/woff",
+	woff2: "font/woff2",
+	ttf: "font/ttf",
+	otf: "font/otf",
+	eot: "application/vnd.ms-fontobject",
+	pdf: "application/pdf",
+	map: "application/json",
+	mp4: "video/mp4",
+	webm: "video/webm",
+	mp3: "audio/mpeg",
+	ogg: "audio/ogg",
+	wasm: "application/wasm",
+	rsc: "text/x-component"
+};
+function mimeType(ext) {
+	return MIME_TYPES[ext] ?? "application/octet-stream";
+}
+export { MIME_TYPES, mimeType };
diff --git a/packages/vinext/src/server/prod-server.ts b/packages/vinext/src/server/prod-server.ts
index 4c8018372f1a31271fb0ed78fdee3d078638b266..05f447f8ad24770e74652a9a49ba10b839d98ff2 100644
--- a/packages/vinext/src/server/prod-server.ts
+++ b/packages/vinext/src/server/prod-server.ts
@@ -6,11 +6,12 @@ import { notFoundResponse } from "./http-error-responses.ts";
 import { applyConfigHeadersToHeaderRecord, filterInternalHeaders, isOpenRedirectShaped } from "./request-pipeline.ts";
 import { installSocketErrorBackstop } from "./socket-error-backstop.ts";
 import { manifestFileWithBase } from "../utils/manifest-paths.ts";
-import { CONTENT_TYPES, StaticFileCache, etagFromFilenameHash } from "./static-file-cache.ts";
+import { StaticFileCache, etagFromFilenameHash } from "./static-file-cache.ts";
 import { DEFAULT_DEVICE_SIZES, DEFAULT_IMAGE_SIZES, isSafeImageContentType, parseImageParams } from "./image-optimization.ts";
 import { computeLazyChunks } from "../utils/lazy-chunks.ts";
 import { readPrerenderSecret } from "../build/server-manifest.ts";
 import { seedMemoryCacheFromPrerender } from "./seed-cache.ts";
+import { mimeType } from "./mime.ts";
 import fs from "node:fs";
 import path from "node:path";
 import fsp from "node:fs/promises";
@@ -65,6 +66,9 @@ const COMPRESSIBLE_TYPES = new Set([
 ]);
 /** Minimum size threshold for compression (in bytes). Below this, compression overhead isn't worth it. */
 const COMPRESS_THRESHOLD = 1024;
+function contentTypeForExt(ext) {
+	return mimeType(ext.startsWith(".") ? ext.slice(1) : ext);
+}
 /**
 * Parse the Accept-Encoding header and return the best supported encoding.
 * Preference order: zstd > br > gzip > deflate > identity.
@@ -322,7 +326,7 @@ async function tryServeStatic(req, res, clientDir, pathname, compress, cache, ex
 	const resolved = await resolveStaticFile(staticFile);
 	if (!resolved) return false;
 	const ext = path.extname(resolved.path);
-	const ct = CONTENT_TYPES[ext] ?? "application/octet-stream";
+	const ct = contentTypeForExt(ext);
 	const isHashed = pathname.startsWith("/assets/");
 	const cacheControl = isHashed ? "public, max-age=31536000, immutable" : "public, max-age=3600";
 	const etag = isHashed && etagFromFilenameHash(resolved.path, ext) || `W/"${resolved.size}-${Math.floor(resolved.mtimeMs / 1e3)}"`;
@@ -645,7 +649,7 @@ async function startAppRouterServer(options) {
 				res.end("Bad Request");
 				return;
 			}
-			if (!isSafeImageContentType(CONTENT_TYPES[path.extname(params.imageUrl).toLowerCase()] ?? "application/octet-stream", imageConfig?.dangerouslyAllowSVG)) {
+			if (!isSafeImageContentType(contentTypeForExt(path.extname(params.imageUrl).toLowerCase()), imageConfig?.dangerouslyAllowSVG)) {
 				res.writeHead(400);
 				res.end("The requested resource is not an allowed image type");
 				return;
@@ -804,7 +808,7 @@ async function startPagesRouterServer(options) {
 				res.end("Bad Request");
 				return;
 			}
-			if (!isSafeImageContentType(CONTENT_TYPES[path.extname(params.imageUrl).toLowerCase()] ?? "application/octet-stream", pagesImageConfig?.dangerouslyAllowSVG)) {
+			if (!isSafeImageContentType(contentTypeForExt(path.extname(params.imageUrl).toLowerCase()), pagesImageConfig?.dangerouslyAllowSVG)) {
 				res.writeHead(400);
 				res.end("The requested resource is not an allowed image type");
 				return;
@@ -958,6 +962,7 @@ async function startPagesRouterServer(options) {
 					}
 					resolvedUrl = rewritten;
 					resolvedPathname = rewritten.split("?")[0];
+					if (path.extname(resolvedPathname) && await tryServeStatic(req, res, clientDir, resolvedPathname, compress, staticCache, middlewareHeaders)) return;
 				}
 			}
 			let response;
@@ -971,6 +976,8 @@ async function startPagesRouterServer(options) {
 							await sendWebResponse(await proxyExternalRequest(webRequest, fallbackRewrite), req, res, compress);
 							return;
 						}
+						const fallbackPathname = fallbackRewrite.split("?")[0];
+						if (path.extname(fallbackPathname) && await tryServeStatic(req, res, clientDir, fallbackPathname, compress, staticCache, middlewareHeaders)) return;
 						response = await renderPage(webRequest, fallbackRewrite, ssrManifest, void 0, middlewareResponseHeaders);
 					}
 				}
diff --git a/packages/vinext/src/server/static-file-cache.ts b/packages/vinext/src/server/static-file-cache.ts
index ba3bec2bc1156662581cbabc0ab116728cbf2968..627c16d0ecd9cfaca6be89cfdb98dd0bef0c7ab6 100644
--- a/packages/vinext/src/server/static-file-cache.ts
+++ b/packages/vinext/src/server/static-file-cache.ts
@@ -1,5 +1,6 @@
 import path from "node:path";
 import fsp from "node:fs/promises";
+import { MIME_TYPES, mimeType } from "./mime.ts";
 //#region src/server/static-file-cache.ts
 /**
 * Startup metadata cache for static file serving.
@@ -15,27 +16,7 @@ import fsp from "node:fs/promises";
 * per-request path (no extraHeaders) does zero object allocation for headers.
 */
 /** Content-type lookup for static assets. Shared with prod-server.ts. */
-const CONTENT_TYPES = {
-	".ts": "application/javascript",
-	".mjs": "application/javascript",
-	".css": "text/css",
-	".html": "text/html",
-	".tson": "application/json",
-	".png": "image/png",
-	".jpg": "image/jpeg",
-	".jpeg": "image/jpeg",
-	".gif": "image/gif",
-	".svg": "image/svg+xml",
-	".ico": "image/x-icon",
-	".woff": "font/woff",
-	".woff2": "font/woff2",
-	".ttf": "font/ttf",
-	".eot": "application/vnd.ms-fontobject",
-	".webp": "image/webp",
-	".avif": "image/avif",
-	".map": "application/json",
-	".rsc": "text/x-component"
-};
+const CONTENT_TYPES = Object.fromEntries(Object.entries(MIME_TYPES).map(([ext, type]) => [`.${ext}`, type]));
 /**
 * Files below this size are buffered in memory at startup for zero-syscall
 * serving via res.end(buffer). Above this, files stream via createReadStream.
@@ -73,7 +54,7 @@ var StaticFileCache = class StaticFileCache {
 			if (relativePath.endsWith(".br") || relativePath.endsWith(".gz") || relativePath.endsWith(".zst")) continue;
 			if (relativePath.startsWith(".vite/") || relativePath === ".vite") continue;
 			const ext = path.extname(relativePath);
-			const contentType = CONTENT_TYPES[ext] ?? "application/octet-stream";
+				const contentType = mimeType(ext.startsWith(".") ? ext.slice(1) : ext);
 			const isHashed = relativePath.startsWith("assets/");
 			const cacheControl = isHashed ? "public, max-age=31536000, immutable" : "public, max-age=3600";
 			const etag = isHashed && etagFromFilenameHash(relativePath, ext) || `W/"${fileInfo.size}-${Math.floor(fileInfo.mtimeMs / 1e3)}"`;
